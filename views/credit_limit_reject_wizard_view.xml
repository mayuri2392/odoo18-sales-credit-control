<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_credit_limit_reject_wizard" model="ir.ui.view">
        <field name="name">credit.limit.reject.wizard.form</field>
        <field name="model">credit.limit.reject.wizard</field>
        <field name="arch" type="xml">
            <form string="Reject Credit Limit Request">
                <group>
                    <field name="partner_id" invisible="1"/>
                    <field name="company_id" invisible="1"/>

                    <field name="reason_id"
                           options="{'no_create': True, 'no_create_edit': True}"/>
                    <field name="reason_note" placeholder="Enter details..."/>
                </group>

                <footer>
                    <button string="Reject"
                            name="action_confirm_reject"
                            type="object"
                            class="btn-primary"/>
                    <button string="Cancel"
                            special="cancel"
                            class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>